default_install_hook_types: [pre-push]
repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.15.13
  hooks:
    - id: ruff-check
      args: [--fix]
      stages: [pre-push]
      types_or: [python, pyi]
    - id: ruff-format
      stages: [pre-push]
      types_or: [python, pyi]
